# modifier-override progress

## Current state

- Swift package scaffold created.
- Local build/install/signing script added.
- Current MVP supports `tab + 0-9 -> option + 0-9`.
- `cmd-tab` and other modified Tab shortcuts are preserved.
- Event tap stability was improved by removing the periodic tap restart.

## Pending

- Add true `caps_lock` support via lower-level HID handling.
- Add app icon/logo.
